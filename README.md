# Targetfinder
This repository contains all of the setup necessary to generate synthetic data for training a yolo model to idenfity a target on a given background. The default will be to identify a generated country flag lying in grass.
